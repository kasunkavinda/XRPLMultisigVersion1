import Account from "../schema/Account";
declare type SignedObject = {
    type: "SignedTx" | "MultiSignedTx";
    id: string;
    signedTransaction: string;
    txJson: Record<string, unknown>;
    signers: string[];
};
declare const sign: (transaction: Object, account?: Account | Account[] | undefined) => SignedObject;
export { sign };
export type { SignedObject };
export default sign;
